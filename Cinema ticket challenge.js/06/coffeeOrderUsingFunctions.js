/*
const coffeeOrder = (size, typeOfDrink) => {
    console.log(`Size ${size} Type ${typeOfDrink}`);
}

coffeeOrder ("Tall" ,"Latte");

let coffeeOption = ["coffee", "latte", "mocha"]; //global variable
let coffeeSize = ["small", "medium", "large"];
const makeCoffee = (size,type) => {
    console.log(` size coffee is ${size} and type of drink is ${type}`);
}
makeCoffee(coffeeSize[0],coffeeOption[1]); //using arrays declared outside function
makeCoffee(coffeeSize[2],coffeeOption[0]);

const factorial = (n) => {
    if ((n === 0) || (n === 1)) {
    return 1;
    } else {
    return (n * factorial(n-1));
    }
}

console.log(factorial(33));


let orderCount = 0;

const takeOrder = (topping, base) => {
 console.log(`Order number ${orderCount} Pizza with ${topping} on ${base} base`);
 orderCount++;
}
takeOrder("pineapple", "Thick");
takeOrder ("Mushroom", "Thin")
*/